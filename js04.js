let num1 = parseInt(prompt("Añade primer numero"));

let num2 = parseInt(prompt("Añade otro numero mas"));
suma(num1, num2);


function suma(num1, num2) {
    let result = num1 + num2;
    let total = result;

    console.log(total);
}